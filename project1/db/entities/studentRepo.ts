/**
import {Student} from './students';
module.exports = (db) =>{

  const findStudentByid = (id) =>{
    const student = await student.findByPk(123);
    if (student === null) {
      console.log('Not found!');
    } else {
      return student;
    }
  }



}
**/
